import React from 'react';
import LoginScreen from '../screens/auth/login';

import {
    StyleSheet,
    Text,
    View,
    Button,
    SafeAreaView,
    ScrollView,
    ImageBackground,
    TextInput
  } from 'react-native';

export default function index() {
  return (
    <LoginScreen/>
  )
}
